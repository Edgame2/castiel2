# =============================================================================
# Castiel Web Application - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values for your environment.
# Never commit .env.local files to version control!
#
# Note: Next.js automatically loads .env.local, .env.development, .env.production
# For production deployments, use Azure App Service configuration or similar.
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Base URL of the Castiel API service
# Required in production, defaults to http://localhost:3001 in development
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# API port (used for Next.js rewrites in development)
# Optional - defaults to 3001
API_PORT=3001

# -----------------------------------------------------------------------------
# OAuth Configuration
# -----------------------------------------------------------------------------
# OAuth client ID for authentication
# Optional - only needed if using OAuth login
NEXT_PUBLIC_OAUTH_CLIENT_ID=

# OAuth redirect URI (must match OAuth provider configuration)
# Optional - only needed if using OAuth login
NEXT_PUBLIC_OAUTH_REDIRECT_URI=

# OAuth scope (space-separated list of scopes)
# Optional - only needed if using OAuth login
NEXT_PUBLIC_OAUTH_SCOPE=

# -----------------------------------------------------------------------------
# Application Insights / Monitoring
# -----------------------------------------------------------------------------
# Application Insights connection string (preferred)
# Optional - only needed if monitoring is enabled
NEXT_PUBLIC_APP_INSIGHTS_CONNECTION_STRING=InstrumentationKey=your-key;IngestionEndpoint=https://your-region.in.applicationinsights.azure.com/

# Application Insights instrumentation key (alternative to connection string)
# Optional - only needed if monitoring is enabled and connection string is not provided
# NEXT_PUBLIC_APP_INSIGHTS_INSTRUMENTATION_KEY=your-instrumentation-key

# Enable analytics tracking
# Optional - set to 'true' to enable analytics
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# -----------------------------------------------------------------------------
# Google Analytics (Optional)
# -----------------------------------------------------------------------------
# Google Analytics tracking ID (e.g., G-XXXXXXXXXX)
# Optional - only needed if using Google Analytics
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Analytics endpoint for custom analytics
# Optional - only needed if using custom analytics endpoint
# NEXT_PUBLIC_ANALYTICS_ENDPOINT=

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
# Application environment (development, staging, production)
# Optional - defaults to NODE_ENV
# NEXT_PUBLIC_APP_ENV=development

# -----------------------------------------------------------------------------
# Bundle Analyzer
# -----------------------------------------------------------------------------
# Enable bundle analyzer (set to 'true' to enable)
# Optional - only for development/debugging
# ANALYZE=false

# =============================================================================
# Notes:
# =============================================================================
# 1. All NEXT_PUBLIC_* variables are exposed to the browser and should not
#    contain sensitive information.
#
# 2. For production deployments:
#    - Set NEXT_PUBLIC_API_BASE_URL to your production API URL
#    - Configure Application Insights for monitoring
#    - Set up OAuth providers if using OAuth login
#
# 3. For local development:
#    - Use the default values (localhost:3001 for API)
#    - OAuth and monitoring are optional
#
# 4. Environment variable precedence (Next.js):
#    - .env.local (loaded in all environments, ignored by git)
#    - .env.development / .env.production (loaded based on NODE_ENV)
#    - .env (loaded in all environments)
# =============================================================================
