# Revimize dev (api.revimize-dev.com + www.revimize-dev.com)
# Copy to .env or set in your deployment; do not commit secrets.

# --- API Gateway (docker-compose api-gateway) ---
# CORS: allow UI origin so browser can call api.revimize-dev.com from www.revimize-dev.com
FRONTEND_URL=https://www.revimize-dev.com

# --- Auth (docker-compose auth) ---
# Public URL of the API (gateway); used for OAuth/SAML callback URLs
BASE_URL=https://api.revimize-dev.com
# Where to redirect users after login (web UI)
MAIN_APP_URL=https://www.revimize-dev.com

# --- UI build (set when building the Next.js app) ---
# Browser will call this for all API requests
NEXT_PUBLIC_API_BASE_URL=https://api.revimize-dev.com
