/**
 * Placeholder test so vitest run tests/unit finds at least one test.
 * Phase 4 will add real unit tests for services.
 */
import { describe, it, expect } from 'vitest';

describe('llm-service', () => {
  it('has unit test suite', () => {
    expect(true).toBe(true);
  });
});
