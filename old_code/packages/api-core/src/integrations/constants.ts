/**
 * Integration Constants
 * Centralized constants for integration system
 */

/**
 * Maximum number of user connections per integration
 * This limit prevents resource abuse and ensures fair usage
 */
export const MAX_USER_CONNECTIONS_PER_INTEGRATION = 50;
