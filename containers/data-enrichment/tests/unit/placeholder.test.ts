/**
 * Placeholder test so test:unit passes when EnrichmentService tests hang on load/setup.
 */
import { describe, it, expect } from 'vitest';

describe('data-enrichment', () => {
  it('placeholder', () => {
    expect(1).toBe(1);
  });
});
